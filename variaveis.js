// Cálculo IMC

let altura = 1.70
let peso = 70 

const pi = 3.14 // variável constante

let r = peso / (altura * altura)

let teste = pi * 2

console.log("O resultado é: " + r)
console.log("O resultado é: " + teste) 

/*

    Renomenado variaveis:
        1. Não inicar com número, apenas letras ou _
        2. Não usar espaços
        3. Não usar caracteres especiais, apenas _ e $
        4. Não usar palavras reservadas (var, let, const, function, if, else, etc)


        5. Usar camelCase (alturaPessoa, pesoPessoa, etc)
        6. Variáveis são case sensitive (altura é diferente de Altura)

*/

